---
"@savvy-web/lint-staged": patch
---

## Other

- Migrate Effect dependencies to `catalog:silk` for centralized version management via pnpm-plugin-silk.
- Replace `jsonc-parser` with `jsonc-effect` for Effect-native JSONC parsing and modification.
